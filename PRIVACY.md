# 隐私说明

生效日期：2026-09-13

AI Chat to Notion 是一个在用户浏览器本地运行的开源扩展。本说明解释扩展会接触哪些数据、数据会被保存在哪里，以及数据会发送给谁。

## 一眼看懂

可以把扩展理解为用户亲自控制的“搬运工具”：它从当前 Claude 或 ChatGPT 页面读取你能看到的对话，在你点击导出后，把选中的内容交给 Notion。项目没有开发者服务器，不收集统计数据，也不销售或共享用户数据。

## 扩展会读取什么

扩展只在以下受支持的网站运行内容脚本：

- `https://claude.ai/*`
- `https://chatgpt.com/*`
- `https://chat.openai.com/*`

为了生成可选择的问答列表，扩展会读取当前网页 DOM 中已经显示或在扫描过程中渲染的内容，包括：

- 用户问题和 AI 回答的文字与结构；
- 标题、列表、引用、代码、表格、链接和数学公式等格式信息；
- 无法直接导出的图片或附件是否存在，用于生成占位说明；
- 当前页面地址，用于在当前标签页内区分不同来源会话。

扩展不会读取或导出隐藏推理过程，也不会上传图片和附件文件本身。

## 保存在浏览器里的数据

以下设置通过 `chrome.storage.local` 保存在安装扩展的本地浏览器配置中：

- `notionToken`：用户填写的 Notion Integration Token；
- `defaultPageId`：可选的默认 Notion 父页面 ID；
- `defaultPageTitle`：默认页面的显示名称；
- `btnLeft`、`btnTop`：网页浮动按钮的位置。

`chrome.storage.local` 是浏览器扩展的本地存储区域。本项目没有在这些值之外增加额外的加密层，因此任何能控制本机浏览器配置或扩展调试环境的人，都可能读取其中的 Token。请保护好操作系统账户，不要把浏览器用户数据目录分享给他人。

以下数据只保存在当前网页标签页的内存中，不写入磁盘存储：

- ChatGPT 对话扫描得到的临时消息快照；
- 当前来源页面最近一次成功导出的 Notion 页面和插入位置；
- 用于判断会话切换、回答更新和分支变化的临时状态。

刷新或关闭标签页后，这些内存数据会消失。切换到另一段 ChatGPT 会话时，旧会话缓存会被清理。

## 会发送到哪里

扩展只主动请求 Notion 官方 API：`https://api.notion.com/*`。

根据用户操作，请求中可能包含：

- Notion Integration Token，用于身份验证；
- Notion 页面 ID、页面标题和页面内容块；
- 用户主动选中的对话标题与正文；
- 用户选择的插入位置。

这些数据用于搜索用户已授权的页面、预览页面结构和创建导出内容。数据的处理同时受 Notion 自己的服务条款和隐私政策约束。

扩展不会把对话发送给项目作者或第三方统计平台，也不会调用 Claude API、Anthropic API、OpenAI API、广告接口或遥测服务。Claude、ChatGPT 和 Notion 网页本身如何处理数据，由相应服务各自的政策决定，不属于本扩展能够控制的范围。

## 权限用途

- `storage`：保存 Notion Token、默认页面信息和浮动按钮位置。
- `activeTab`：在用户当前使用的标签页中配合扩展交互。
- `https://api.notion.com/*`：通过 Notion API 搜索授权页面、读取页面内容块并写入导出结果。
- Claude 和 ChatGPT 网站匹配规则：在这些网页中读取对话并显示导出按钮和面板。

## 如何清除或撤销

- 删除已导出的内容：在 Notion 中删除相应页面或内容块。
- 阻止继续访问某个页面：在该 Notion 页面的 Connections 设置中移除 Integration。
- 让旧 Token 失效：在 Notion 的 Integration 设置中撤销或重新生成 Secret。
- 清除扩展本地数据：在浏览器的扩展管理中删除本扩展及其站点数据，然后按需重新安装。
- 清除临时聊天缓存：刷新或关闭对应的 Claude / ChatGPT 标签页。

## 开源代码与问题报告

所有数据处理逻辑都可以在本仓库中检查。报告问题时，请使用虚构示例或经过脱敏的内容，不要提交 Notion Token、页面 ID、私人页面名称、真实聊天记录或包含这些信息的截图。

如果未来版本新增服务器、统计分析、OAuth 服务或新的数据用途，本说明应在发布前同步更新。
